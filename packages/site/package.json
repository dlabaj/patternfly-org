{
  "name": "site",
  "type": "module",
  "version": "0.0.1",
  "scripts": {
    "dev": "astro dev",
    "clean": "rm -rf src/generated && npm run generate:content",
    "build": "astro build",
    "preview": "astro preview",
    "generate:content": "tsx generateContent.ts",
    "astro": "astro",
    "start": "patternfly-doc-core start",
    "build:docs": "patternfly-doc-core build",
    "serve": "patternfly-doc-core serve",
    "sync": "patternfly-doc-core sync",
    "init:docs": "patternfly-doc-core init"
  },
  "dependencies": {
    "astro": "^5.7.13"
  },
  "devDependencies": {
    "@patternfly/patternfly-doc-core": "^1.7.0",
    "tsx": "^4.19.0"
  }
}
